apiVersion: v1
kind: Service
metadata:
  name: potion-shop-laravel-k8s
  labels:
    app: potion-shop-laravel-k8s
spec:
  type: NodePort
  selector:
    app: potion-shop-laravel-k8s
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
    protocol: TCP
    name: http
